/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['sfycdn.speedsize.com', 'cdn.shopify.com'], // Buraya kullanacağınız domainleri ekleyin
    },
  };

export default nextConfig;
